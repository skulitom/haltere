# Session checkpoint — 2026-09-23

Work stopped at the user's request to conserve weekly usage. Liftoff was left
open and paused inside hidden Anode; the virtual pad was released only after
live telemetry confirmed the pause action. No flight or training job was left
running by this task. Retain the original `[Copy] New Drone`.

## Resume here

1. Finish automatic monitoring: the new `flight_monitor` reports progress and
   stalls without screenshots. Add validated result-screen recognition and
   safe automatic stopping/reset handling. Review images only for ambiguous
   results and failure diagnosis. See [monitoring](flight_monitoring.md).
2. Complete the remaining `03-dense-brain` condition in the frozen batch
   `runs/dense-geometry-comparison-20260923`. It has **not been launched**.
   `run_condition.py` checks every frozen source hash before starting. Controller
   runtime is `6d365a0`; do not change it for this last condition. The existing
   pad job has ended: attach a new pad, verify real processed controls at idle
   and with short pulses, and reset the paused course before proceeding.
   Any new observer must be disclosed and its overhead checked before flight.
3. Diagnose the planner's descent to the ground at a wall. More dense obstacle
   points alone did not fix successful navigation. Use causal local task
   guidance and visible clearance; no course-specific route or tuning.
4. Resume broader generalization and speed work only after those diagnostics.
   The five-track, three-clean-races-each, within-20% target remains unmet.

## Frozen comparison so far

Known generated box course, one lap, original drone, 2.5 m/s; motor10 candidate05
is the unchanged brain. One planned attempt per condition, not three repeats.

| Condition | Result | Observed game clock |
|---|---|---|
| Sparse geometry + PD motors | Wall impact; incomplete | 2:17.411 |
| Dense geometry + PD motors | Ground contact, then declared stall; incomplete | 5:05.407 |
| Dense geometry + brain motors | Not run | — |

Both completed videos fully decode. Neither had a camera or controller-deadline
failure. The first stopped automatically on impact; the second was paused by
the operator under the predeclared 60-second/1-metre rule. Complete raw logs,
videos, original/amended preflight cards and reviews remain in the batch folder.
These two new videos have not yet been published; publish all three together
after the remaining condition and verification. Do not count this as a completed
comparison or an unseen-track result.

The original ground-check attempt passed its control checks but failed to save
its report due to a duplicated absolute path. Its output and original cards are
preserved. The corrected check passed before either flight; all manifests record
that preflight amendment. The controller was unchanged.

## Models and evidence

The latest published experimental brain remains
`runs/motor-brain-10-tracking-05/candidate.pt`, SHA256
`64444a628c58b2c1615086bc84f8c5d3c0a0a12667df31d888c4587a802288b2`.
None of the new flight-cost brains beat their parent in matched Liftoff motor
tests. No new brain weights are ready for promotion or upload.

The optional dense input is a repackaged pretrained perception model, not a
trained brain. Its export, SHA256 and source revision are recorded in
`runs/depth-completion-probe-20260923/metric-indoor-336.ts.json`.
Its metric scale is unqualified for Liftoff. See
[dense geometry](experimental_dense_geometry.md) for limits.

The previous three-attempt geometry batch finished 2/3 with PD motors. All three
untrimmed videos and evidence are [published and hash-verified](https://github.com/skulitom/haltere/releases/tag/visual-geometry-consistent-depth-20260923).
The matched flight-cost03 motor comparison is also
[published and verified](https://github.com/skulitom/haltere/releases/tag/motor-flightcost03-comparison-20260923).

Use Anode `C:/DEV/Anode/artifacts/live-main/anode.exe` (verified 0.8.0), channel
`main`, agent `haltere-race-eval`; reacquire its lease and inspect fresh state.
Never launch an older build or bring up its viewer by default. LitHarness has
the user's explicit coexistence exception; heavy training/tests remain outside
measured flight time. Preserve unrelated `configs/explore_spiral.yaml`.
