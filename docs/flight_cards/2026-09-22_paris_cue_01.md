# Paris Drone Festival / City Trip: camera freshness stop

First project exposure in the inspected inventories: a whole-word Paris / Drone
Festival search found no references in project documentation, configurations or
local JSON/JSONL/YAML training and flight inventories before selection.
No course geometry, route or coordinates were inspected or supplied.

Preflight manifest: `runs/race-completion-20260922/paris-cue-01-manifest.json`.
Code `f880ae7`, unchanged scene-09 brain, detector and race-cue controller;
nominal speed 2 m/s. The new common stop bounds are 250 m above launch,
2000 m horizontal distance and 10 m/s. Criterion: full game-confirmed race
within 1700 seconds, without contact, reset or intervention. The course uses
three laps. Original drone; hidden Anode 0.6.0.

## Result

**Incomplete: camera freshness guard paused the test.** Game clock 01:27.227,
lap 1/3. No impact or estimated collision, no controller deadline failure.
Maximum image age was 252.516 ms, exceeding the 250 ms memory allowance.
The run ended after 137.9 seconds of controller wall time. It did not complete
a lap and is counted as an unsuccessful full-race attempt.

Standard brain/gameplay video, telemetry and metadata are preserved under
`runs/race-completion-20260922/paris-cue-01*`. A separately manifested repeat,
`paris-cue-02`, keeps all code, models, parameters and game settings unchanged;
no background video decoding is run alongside that attempt. This tests whether
the timing interruption recurs; it does not assume that decoding caused it.

The unchanged repeat also stopped on camera freshness, after only 4.1 seconds
of controller wall time (253.718 ms image age), before the race timer started.
It detected no impact. These results motivated bounded camera-outage braking
and a two-thread OpenCV limit. Paris is now development data. The separately
manifested `paris-cue-03` is a 200-second regression, not an untouched race test.
It completed its planned duration without contact, reset, camera outage or
controller deadline failure; the game was on lap 1/3 at 02:30.546. Maximum image
age was 165.601 ms, so the extra braking interval was not exercised in that
flight. Automated checks cover its stale-image, braking and hard-stop behavior.
