# 2026-09-17 strawbale 1 — does today's pilot still fly the home lap?

Written before the flight.

- **Rung**: Z0 (home map, home gates, in the detector's training set). A regression test, not a
  generalisation number.
- **Track**: Straw Bale / "Field Day" (pool: home)
- **Brain**: `artifacts/ftPath2_best.pt`
- **Detector**: `runs/gatenet9/best.pt` sha256 `a00d0252e0…`
- **Pilot**: main at `8aec50b`. Changed since the last game flights (w22/w23, clean 7/7 on gatenet9):
  course-order selection with the `start_line_w` prior, the climb-only height floor, the measured
  optic-flow clearance, the merged tracker fixes, and the contact rule in the scorer. **None of these
  has flown in the game.** The one that reaches the brain's own inputs is the clearance: it latches
  the first gate's crossing height, which on this course is about 1.2 m, the same as the old prior —
  so it should change nothing here, and if the speed looks different, that is the first suspect.
- **Command**:
  ```
  haltere liftoff fly artifacts/ftPath2_best.pt --vision runs/gatenet9/best.pt --camera configs/camera_seat.yaml
      --sight rabbit --sight-speed 3.5 --sight-gate-speed 3.2 --sight-turn-gate-speed 2.8
      --sight-flow-min 0.6 --sight-flow-alt ground --throttle-scale 0.8 --gyro telemetry --reset-key R
      --log data/liftoff/logs/w27_regression.csv
  ```
  Identical to w22/w23 except for the pilot code, so the pilot is the only variable.
- **Course parameters typed**: none.
- **Prediction**: 7/7 on the first attempt, gate 0 -> 6 in 55-65 s (w22 60 s, w23 62 s), every
  lateral within 0.5 m. If it loses a gate, the course-order prior is the likeliest cause, because it
  changes which arch is chosen at the start.
- **Pass criterion**: 7/7 on at least one of the first two attempts with every lateral within 0.5 m
  and no contact at a gate. Anything less is a regression, and nothing from today ships until it is
  explained.

## Result

**FAILED the pass criterion. Nothing from today's pilot ships on this evidence.**

- HUD/scorer: attempt 1 gates [0, 1, 2] = **3/7**; attempt 2 [0, 1] before the time budget ended it.
  Against w22/w23's 7/7 on the same course, camera and detector.
- Gates 0-2 were clean (+0.2, +0.1, +0.6 m). **Gate 3 was never flown at**, and gate 4's plane was
  crossed 7.8 m and 7.0 m wide. Two contacts at 203-204 s, long after the course was lost.
- Shake: yaw **21.0 deg/s** against w23's 2.1, body rates 14.7 against 4.7. The nose was swinging
  ten times harder than on the good lap.

What the replay says (`liftoff replay-sight`, which reproduces this flight to 100% on target estimate
and n_passes, so its account is trustworthy):

- **Arch 3 was never tracked at all.** The pilot did not choose the phantom over gate 3; gate 3 was
  never acquired, so it was never a candidate.
- After gate 2 the target became **phantom #45** at (81.2, 36.4) - four sightings that
  `saw {'none': 4}`, the detector firing confidently where no arch stands - held as the target for
  **683 frames**, and then *declared a pass*. Then phantom #47 at (67.0, 59.8) for **1184 frames**,
  also declared a pass.
- 17 confirmed phantoms against w23's 8; 100 tracks created against 58; 18 target switches against
  12; 11 passes declared of which **7 were not at any arch**.

The chain that fits every number: more phantoms -> more target switches -> the rabbit's heading jumps
-> the nose swings (the 21 deg/s) -> detections smear across bearings -> more phantoms. A loop, not a
single bad decision, which is why the bench never showed it: the rehearsal's synthetic detector has
no false positives to feed it.

Next: fly the same command with the course-order merge reverted, to find out which of today's two
pilot merges starts the loop.
