# 2026-09-17 strawbale 2 — which of today's two pilot merges starts the phantom loop?

Written before the flight.

- **Rung**: Z0. A bisection, not a result.
- **Track**: Straw Bale / "Field Day" (home)
- **Brain**: `artifacts/ftPath2_best.pt` · **Detector**: `runs/gatenet9/best.pt`
- **Pilot**: main with the **course-order merge (42cdedb) reverted**. Everything else from today
  stays: the tracker fixes, the measured flow clearance, the climb-only height floor, the contact
  rule. 103 tests pass.
- **Command**: identical to flight 1 except `--log data/liftoff/logs/w28_no_order.csv`.
- **Course parameters typed**: none.

**Why this one first.** Flight 1 lost the course after gate 2 and never tracked arch 3, chasing two
phantoms that it then booked as passes. Two merges landed on the pilot today and either could start
that: the course-order selection, or the tracker fixes. Course order is the more suspect of the two —
it is the one that decides *which* arch to fly at, it is the one whose own bench run regressed narrow
and wide, and it carries a new prior about where a race starts. The tracker fixes, by contrast, were
measured against six real game flights and reduced phantom-driven target deletions there.

- **Prediction**: better than 3/7 but I do not expect a clean lap. If course order alone is the
  cause, gate 3 gets tracked and flown and this reads 6/7 or 7/7, with yaw shake back near 2-5 deg/s.
  If it reads 3/7 again with yaw still around 20 deg/s, the tracker merge is implicated instead and
  the next flight reverts that one.
- **Pass criterion**: this flight is diagnostic — it passes by *discriminating*, not by scoring well.
  The number that decides it is **arch 3 tracked or not**, and the yaw shake.

## Result

**Discriminated, and it is not one merge but both.** Gates [0, 1, 2, 5, 6] = **5/7**, up from 3/7.

The bisection worked: gate 3 was *engaged* this time - crossed 2.0 m off centre at 58 s with a
contact at (59, 42), which is gate 3's own position - where flight 1 never tracked arch 3 at all. So
the course-order merge was really costing the course after gate 2, and reverting it bought two gates.

But **yaw shake is still 18.0 deg/s** against w23's 2.1, and gate 4 was still missed by 2.1 m. The
phantom loop is still running, so the course-order merge was not the only thing feeding it. The
remaining suspect is the tracker merge, which landed the same day and is the other thing that changed
how tracks are confirmed, retired and booked as passes.

Next: fly the same command with the tracker fixes switched OFF by parameter rather than reverted -
their own review documented the switches, and reverting the merge would take the replay tool with it.
