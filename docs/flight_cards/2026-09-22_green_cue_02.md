# The Green / course 01: horizontal-hold repeat

Development repeat, frozen runtime `de744e8`, scene-09 brain and matching
detector, original drone, unchanged 2 m/s assistance. Preflight manifest:
`runs/race-completion-20260922/green-cue-02-manifest.json`.
Prediction: horizontal holding during clipped upper-marker recovery would avoid
advancing into the building. Criterion: game-confirmed full two-lap race within
1700 s without contact, reset or intervention. The 51 focused tests passed.

**Failed: another impact beneath the overhang, lap 1/2.** Controller wall time
453.86 s, game time 07:21.514, airborne time 448.06 s. Median speed 1.873 m/s;
90th percentile 2.064 m/s. The terminal motion guard recorded 63.44 m/s²
unexplained acceleration. The CSV contact list is empty, but the scorer retains
the terminal impact; an empty list must not be reported as a clean flight.
Maximum image age was 367.69 ms, within bounded braking; timing did not stop it.

The standard brain/gameplay video, telemetry, manifest and score are preserved
under `runs/race-completion-20260922/green-cue-02*`. This result does not verify
the horizontal-hold change as a repair. Subsequent work follows the
[geometry and frozen-batch program](../generalization_program.md), rather than
another course-specific marker recovery rule. No unseen full finish is claimed.
