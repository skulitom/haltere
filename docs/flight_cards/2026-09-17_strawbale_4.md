# 2026-09-17 strawbale 4 — the release candidate, defaults only

Written before the flight.

- **Rung**: Z0, the regression that gates the release.
- **Brain** `artifacts/ftPath2_best.pt` · **Detector** `runs/gatenet9/best.pt`
- **Pilot**: main at `07ca54a`, **no `--sight-set` at all** — the point is that the shipped defaults
  fly, not that a hand-tuned command does.
- **Prediction**: 7/7, yaw shake 2-3 deg/s, gate 0 -> 6 in 60-65 s. w29 proved the behaviour with the
  switches typed out; this proves the defaults carry it.
- **Pass criterion**: 7/7, no contacts, every lateral within 0.5 m. Below that, nothing is published.

## Result

**7/7, passed.** Gates [0,1,2,3,4,5,6], gate 0 -> 6 in **64 s** at 3.19 m/s, laterals +0.1 +0.2 +0.3
-0.1 +0.4 +0.4 +0.3, **0 collisions**, yaw shake **2.0 deg/s**, body rates 2.1, horizon 0.48 deg.
Footage recorded to `docs/liftoff_by_sight_v05.mp4`.

Four clean by-sight laps now stand on gatenet9 — w22, w23, w29, w30 — against the published
gatenet8's best of 6/7 (w19). That is the detector upgrade this release is built on, and it was
measured in the game rather than on the bench.
